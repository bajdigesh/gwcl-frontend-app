import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
  subTitle: {
    fontSize: '0.875rem',
    fontWeight: 400,
    marginTop: '5px',
  },
  drawerPaddingFix: {
    margin: '0 -24px',
  },
}));
export default useStyles;
