import { makeStyles } from '@material-ui/core';
const useStyles = makeStyles(theme => ({
  footerFixedBtn: {
    position: 'absolute',
    bottom: 0,
    right: 0,
    left: 0,
  },
}));

export default useStyles;
